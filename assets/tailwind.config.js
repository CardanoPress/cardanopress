module.exports = {
    purge: ["./src/*.js", "../templates/*.php"],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
